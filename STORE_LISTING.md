# Chrome Web Store 发布资料

## 单一用途

读取用户正在观看的 B 站视频公开字幕，通过用户自行配置的 LLM API 生成简要总结、章节时间线、亮点摘录，并基于视频内容回答后续问题。

## 简短说明

读取 B 站公开视频字幕，使用你配置的 LLM API 生成总结、时间线、亮点和视频问答。

## 详细说明

B站视频总结助手会嵌入 B 站视频页右栏，优先读取中文字幕，其次读取英文或其他公开字幕。点击“总结”后，扩展通过用户配置的 OpenAI 兼容 LLM API 流式生成：

- 简要总结
- 可点击跳转的章节时间线
- 带时间点的亮点原话
- 基于总结和字幕的后续问答

总结与问答记录只保存在浏览器本地，支持历史查看、重新总结和批量删除。API Key 仅保存在当前浏览器本地，不参与 Chrome 账号同步。扩展不提供模型服务，调用费用和数据处理规则由用户选择的 LLM 服务商决定。

## 权限说明

- `storage`：在本机保存 API Key、总结缓存和问答历史，并同步不敏感的界面设置。
- `bilibili.com`：在视频页显示总结界面并读取视频信息与公开字幕列表。
- `hdslb.com`：读取 B 站返回的字幕文件。
- `bilivideo.com` / `bilivideo.cn`：无字幕时下载 B 站音频并发送给用户配置的转写服务。
- `api.deepseek.com`：调用默认 DeepSeek OpenAI 兼容接口。
- 可选站点权限：只有用户配置其他 Base URL 并点击保存时，才请求该 LLM 服务的单一域名权限；切换服务后移除旧域名权限。

## 远程代码声明

否。扩展不会下载或执行远程 JavaScript、WebAssembly 或其他可执行代码。LLM API 只返回文本数据。

## 数据披露建议

在 Privacy practices 中如实申报：

- Website content：视频标题、公开字幕、视频标识和页面地址。
- User activity：用户主动使用扩展生成总结的当前视频上下文。
- User-provided content：用户输入的问题和选中的引用文字。
- Authentication information：用户自行填写的第三方 LLM API Key，仅存于当前浏览器本地并直接发送给对应服务。

这些数据只用于扩展的单一用途，不用于广告，不出售给第三方，开发者无法访问。总结和问答保存在本地；视频内容和问题仅在用户确认后发送给其配置的 LLM 服务。

## 审核测试说明

1. 安装扩展并打开设置。
2. 填写测试用 LLM API Key，保留默认 Base URL 和 Model Name。
3. 勾选允许发送数据并保存。
4. 打开一个具有公开字幕的 B 站视频。
5. 点击右栏“视频总结”中的“总结”，检查流式总结、时间线跳转、亮点和提问。

若不向审核人员提供测试 API Key，应在审核说明中明确插件采用 BYOK 模式，并附一个可用的临时测试凭据或无需认证的测试端点，否则审核人员可能因无法验证核心功能而拒绝。

## 发布前人工材料

- 128x128 商店图标：`assets/icon-128.png`
- 至少 1 张 1280x800 或 640x400 的功能截图
- 对外可访问的隐私政策 URL，可将 `PRIVACY.md` 发布到项目网站或 GitHub Pages
- 开发者支持邮箱
