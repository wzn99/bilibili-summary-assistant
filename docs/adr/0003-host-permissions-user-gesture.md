# 可选主机权限只在用户手势中申请

用户配置的第三方服务域名权限只能在设置保存的用户手势中申请（设置页保存按钮流程或页内设置弹窗）；Service Worker 只做已授权校验，运行时保存路径显式跳过申请，缺少权限时直接失败。Chrome 会在非用户手势中拒绝 `chrome.permissions.request()`，且未经解释的后台弹窗属于糟糕体验。不要把权限申请移到后台消息或总结流程里"顺手"补上。
