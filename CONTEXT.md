# B站视频总结助手（浏览器扩展）

本上下文定义浏览器扩展内部的业务语言。扩展运行在 B站视频页面中，把字幕或音频转写文本交给用户配置的 LLM 服务，生成可跳转的视频总结。不要把外层 MCP/GUI 项目的概念（总结系列、归档目录）带入本上下文。

## 文本来源

**文本来源**:
实际送入总结模型的文本，值为字幕或音频转写。
_Avoid_: 用"字幕"泛指转写文本

**字幕**:
视频页面上可读的公开字幕，经时间锚点整理后的首选文本来源。

**音频回退**:
没有可用字幕时，在授权后读取登录态音频并交给转写提供商的备用路线。

**转写提供商**:
音频转写服务的类别，仅限 DashScope 异步文件转写与 OpenAI 兼容上传两条路线。

## 视频上下文

**视频上下文**:
一次视频会话的全部状态：视频信息、文本来源、总结、问答与加载状态。视频切换时整体重置，旧上下文的异步结果不得写入新上下文。

**上下文版本**:
标识当前视频上下文的代号，用于丢弃迟到的旧视频任务结果。

**路由键**:
识别当前视频（含分 P、番剧播放页、稍后再看）的页面地址标识。

## 总结与锚点

**时间锚点**:
把模型输出映射回视频时间的媒体时间标记；章节和亮点靠它实现点击跳转。

**章节**:
按时间排列的总结段落，包含标题、摘要和时间锚点。

**亮点**:
带时间点的视频原话引用。

**协议版本**:
总结输出契约的版本号；变更时使旧缓存条目失效。

**总结缓存**:
浏览器本地的总结结果存储，按缓存键读写并限量淘汰，不包含任何凭据。

## 交互

**总结面板**:
注入到 B站页面的用户界面，承载总结、问答、历史与设置入口。

**选区引用**:
用户在页面上选中的文字，作为引用带入提问。

**问答**:
基于视频文本和总结内容的继续提问，保留本地问答历史。

**历史记录**:
本地保存的总结记录集合，支持本地关键词筛选与 AI 语义筛选。

**历史候选短编号**:
发送给模型的临时编号（r1、r2），返回后映射回真实历史记录；模型只能从当前候选中选择，不能编造记录。

## 服务与权限

**数据同意**:
用户允许把字幕、转写文本或问题发送给所配置第三方服务的开关；它决定服务域名是否纳入权限申请。

**可选服务域名**:
用户配置的 LLM 或转写服务对应的站点权限，只能在保存设置的用户手势中申请。

**PCDN 直链**:
playurl 常优先返回的边缘节点音频地址；音频候选排序必须优先官方 bilivideo.com 镜像。
